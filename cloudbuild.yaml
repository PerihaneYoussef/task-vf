steps:
  - name: 'europe-west1-docker.pkg.dev/vf-grp-aib-dev-mirror/dockerhub/cloud-builders/git'
  args: ['clone', '--single-branch', '--branch',
         '$_BRANCH', '$_REPO_URL',
         '--depth', '1',
         '--verbose']
  id: 'Clone Repository'
